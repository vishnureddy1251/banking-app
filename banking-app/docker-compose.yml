version: '3.8'

services:
  banking-app:
    build: ..
    container_name: banking-app
    ports:
      - "8080:8080"
    volumes:
      - banking-data:/app/data
    restart: unless-stopped

volumes:
  banking-data:
    driver: local